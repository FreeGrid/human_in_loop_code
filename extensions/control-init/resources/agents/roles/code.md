#### Code repository: `{{REPOSITORY_ID}}`

Path: `{{REPOSITORY_PATH}}`. Keep only delivered production source, runtime-required resources, package metadata and user-facing product documentation here. This repository may become public, so do not copy private control-plane paths, plans, fixtures, evidence or internal process records into it.
