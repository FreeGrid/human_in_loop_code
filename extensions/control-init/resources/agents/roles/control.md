#### Control repository: `{{REPOSITORY_ID}}`

Path: `{{REPOSITORY_PATH}}`. This repository is the private control plane. Keep planning, tests, fixtures, verification tools, documents, evidence, implementation records and release-management records here. It may inspect, build and test delivery repositories, but those repositories must not depend on it at runtime.
