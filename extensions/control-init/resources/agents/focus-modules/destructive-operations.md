### Destructive operations

Resolve exact targets before changing them. Do not delete repositories or user data, rewrite history, force-push, reset unrelated work, or perform another difficult-to-recover action without explicit authority. Prefer recoverable operations and report what changed.
