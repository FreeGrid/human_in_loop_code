### Preserve unrelated work

Assume pre-existing working-tree changes belong to the user. Inspect both repositories before editing, keep unrelated changes out of scoped commits and never stash, reset, discard or rewrite them merely to obtain a clean tree.
