### Long-term memory and recovery

Use `CONTROL_INDEX.json`, this `AGENTS.md`, focused commits and PR records as durable memory. Do not make repository identity or authority depend on an earlier chat session. Update bindings through the control workspace update workflow when paths or remotes change.
