### Privacy and secrets

Treat control and paper repositories as private by default and code as potentially public. Never copy credentials, private absolute paths, internal evidence or control-only process records into a delivery repository. Do not store tokens, passwords or other secrets in `CONTROL_INDEX.json`.
