### Branch, commit and PR traceability

Work on a feature branch. Make each small, complete and reviewable change a focused commit, then push it. After the coherent feature passes its required validation, open a PR that records rationale and evidence. For multi-repository work, keep separate scoped commits and cross-reference corresponding commit SHAs and verification results in the PR record.
