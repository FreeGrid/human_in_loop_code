### Human gates

Approval of a plan accepts only the plan text; it does not activate implementation. A task starts only when a human explicitly assigns it. Once assigned, carry the scoped task through implementation and verification without asking for routine step-by-step permission. New remotes, broader scope, merge, release and dangerous or destructive actions still require an explicit human decision.
